# Screenshots

Drop your dashboard and terminal evidence here, then reference them from the writeups with relative links, e.g.:

```markdown
![Firewall drop alerts](../../screenshots/firewall-drops.png)
```

## Suggested screenshots to add (you already have these from your lab)

- `wazuh-agents-active.png` — agent list showing Windows + Kali + Ubuntu active
- `firewall-drop-alerts.png` — "Multiple Firewall drop" alerts in the dashboard
- `detection-rules-logtest.png` — `wazuh-logtest` output showing a custom rule firing
- `active-response-triggered.png` — `active-responses.log` showing trigger lines
- `ssh-bruteforce-spike.png` — the authentication_failed spike from the brute-force attack

## Tip

Crop out anything sensitive (real internal IPs are fine for a lab on RFC1918 ranges, but avoid showing credentials or the admin password).
